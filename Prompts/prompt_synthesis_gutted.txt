You are an experienced zoologist.

Your task is to extract observable phenotype traits (morphology, behavior, ecology, sensory capability) that are explicitly stated in the provided context.

You will be given a list of animal species and traits per species.
Your task is to propose observable phenotype traits (morphology, behavior, ecology, sensory capability) that are likely COMMON across most of these species.

IMPORTANT INTERPRETATION RULES:
- A common characteristic may be stated if it is supported for MOST of the species (clear majority) and is not contradicted for any species.
- Lack of mention for a species does NOT count as contradiction.
- Different descriptions referring to the same biological condition (e.g. "Reduced eyes", "Poor visual acuity", "Degenerate optic system") may be treated as equivalent.
- Do NOT invent new characteristics.
- Do NOT infer characteristics that are not supported by at least one species list.

LANGUAGE RULES:
- Avoid anthropomorphism entirely.
- Use concrete, specific phrases.
- Do NOT use abstract category words such as adaptation(s), feature(s), trait(s), behaviour, ecology, habitat, habits, reproduction, lifestyle.
- Choose a single phrasing that best captures the shared biological condition, even if species differ in degree.
- Do NOT include species names in the final phrasing.

OUTPUT RULES (JSON):
- Return a JSON array. Each item has:
  - "trait": a 3-7 word phrase
  - "sources": an array of citation tags found in the input lists
  - "confidence": 1 if the trait is explicitly stated for most species, 0.5 if it is only strongly implied
- Do NOT include prose or headings.
- Cite only tags present in the input lists; do not invent or reformat them.
- Cite all tags of traits used for the bulletpoint.
- Include every supporting source tag from all species lists for each common trait.
- If no sufficiently supported common characteristic can be identified, return an empty JSON array: []

START IMMEDIATELY WITH THE JSON ARRAY.

Extracted species characteristic lists:
{extracted_species_lists}

Answer:
