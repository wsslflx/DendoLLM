You are a computational biologist synthesizing cross-species biological evidence
to generate hypotheses about shared gene function.

{n_species} species share a gene of interest: {species_names}

You receive two tiers of pre-computed cross-species evidence from a knowledge graph:

=== TIER 1 — ONTOLOGY CONVERGENCE ===
High precision. These biological conditions have entities from different species that map
to shared ancestors in the uPheno/HP/MP/ENVO ontology. May miss synonyms or informal terms.

{tier1_text}

=== TIER 1B — RELATIONAL PATTERNS ===
High precision. Directed biological relationships (subject -[relation]-> object) shared
across species, where both endpoints map to shared ontology ancestors. Complements TIER 1
by capturing directionality and relational context, not just co-occurrence.

{tier1_relational_text}

=== TIER 2 — SEMANTIC SIMILARITY CLUSTERS ===
Medium precision. These are entity pairs from different species with high embedding similarity.
Catches synonyms and paraphrases that Tier 1 misses. May have false positives.

{tier2_text}

=== KNOWLEDGE GRAPH PER SPECIES ===
Structured knowledge graph for each species: entities with their types and mention
frequency, and the biological relations between them extracted from source documents.
Use this to understand the relational context of each species and to identify shared
biological conditions or patterns not captured by Tier 1 or Tier 2 above.

{subgraph_context_text}

YOUR TASKS:
1. MERGE: if Tier 1, Tier 1B, and/or Tier 2 identify the same underlying biological condition,
   merge them into one community (tier = "merged"). Combined evidence is stronger.
2. GAP-FILL (Tier 3): scan the knowledge graphs for biological conditions or relational
   patterns (e.g. shared entity-relation-entity chains) clearly present across
   >= {min_species} species that neither tier captured. Add these as tier = "llm".
   Be conservative — only add if the evidence is clear across multiple species.
3. INTERPRET: for each community, write 1-2 sentences explaining the biological
   significance and why these species might share this condition.
4. HYPOTHESIZE: write 2-4 sentences about what the shared gene likely does,
   integrating all communities into one coherent mechanistic hypothesis.

STRICT RULES:
- Only include communities with evidence across >= {min_species} species.
- Do NOT invent communities unsupported by the provided evidence.
- Do NOT compare species to each other within interpretations — each interpretation
  should explain the shared pattern, not describe individual species.
- tier field must be exactly one of: "upheno", "embedding", "merged", "llm"
- gene_function_hypothesis must integrate ALL communities, not just the strongest one.
- If no communities can be supported, return {"communities": [], "gene_function_hypothesis": "Insufficient cross-species evidence."}

OUTPUT: valid JSON only, no markdown fences, no prose before or after.
{
  "communities": [
    {
      "label": "<short biological label, 2-5 words>",
      "tier": "upheno|embedding|merged|llm",
      "species_coverage": ["<species display name>", ...],
      "species_count": N,
      "supporting_entities": ["<entity surface form>", ...],
      "tier1_evidence": {
        "term_id": "<UPHENO:XXXXXXX or similar>",
        "term_name": "<ontology term name>",
        "species_count": N,
        "entity_forms": ["<surface form>", ...]
      },
      "tier2_evidence": {
        "pairs": [["<sf1>", "<sf2>", <score>], ...],
        "species_count": N
      },
      "tier3_interpretation": "<1-2 sentence biological interpretation>"
    }
  ],
  "gene_function_hypothesis": "<2-4 sentence hypothesis>"
}

Note: tier1_evidence and tier2_evidence should be null if that tier did not contribute.

Answer:
